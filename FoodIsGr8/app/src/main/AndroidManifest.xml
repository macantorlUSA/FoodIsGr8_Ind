<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.usa.mintic.ciclo4.foodisgr8">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodIsGr8">
        <activity
            android:name=".controlador.activities.ActivitySplash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".controlador.activities.ActivityProductDetail"
            android:exported="true" />
        <activity
            android:name=".controlador.activities.MainActivity"
            android:exported="true" />
    </application>

</manifest>